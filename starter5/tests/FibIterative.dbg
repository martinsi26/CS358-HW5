 .data # Program at 4.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
4188:CLASS_Object: # ClassDecl at 0.0
  .word mth_Object_hashCode # MethodDeclNonVoid at 0.0
  .word mth_Object_equals # MethodDeclNonVoid at 0.0
  .word mth_Object_toString # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
4208:CLASS_String: # ClassDecl at 0.0
  .word mth_String_hashCode # MethodDeclNonVoid at 0.0
  .word mth_String_equals # MethodDeclNonVoid at 0.0
  .word mth_String_toString # MethodDeclNonVoid at 0.0
  .word mth_String_concat # MethodDeclNonVoid at 0.0
  .word mth_String_substring # MethodDeclNonVoid at 0.0
  .word mth_String_length # MethodDeclNonVoid at 0.0
  .word mth_String_charAt # MethodDeclNonVoid at 0.0
  .word mth_String_compareTo # MethodDeclNonVoid at 0.0
4240:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
4244:CLASS_Lib: # ClassDecl at 0.0
  .word mth_Object_hashCode # MethodDeclNonVoid at 0.0
  .word mth_Object_equals # MethodDeclNonVoid at 0.0
  .word mth_Object_toString # MethodDeclNonVoid at 0.0
  .word mth_Lib_readLine # MethodDeclNonVoid at 0.0
  .word mth_Lib_readInt # MethodDeclNonVoid at 0.0
  .word mth_Lib_readChar # MethodDeclNonVoid at 0.0
  .word mth_Lib_printStr # MethodDeclVoid at 0.0
  .word mth_Lib_printBool # MethodDeclVoid at 0.0
  .word mth_Lib_printInt # MethodDeclVoid at 0.0
  .word mth_Lib_intToString # MethodDeclNonVoid at 0.0
  .word mth_Lib_intToChar # MethodDeclNonVoid at 0.0
4288:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
4296:CLASS_RunMain: # ClassDecl at 0.0
  .word mth_Object_hashCode # MethodDeclNonVoid at 0.0
  .word mth_Object_equals # MethodDeclNonVoid at 0.0
  .word mth_Object_toString # MethodDeclNonVoid at 0.0
4308:END_CLASS_RunMain: # ClassDecl at 0.0
# ****** class Main ****** # ClassDecl at 4.7
  .byte 205 # 'M' with high bit set # ClassDecl at 4.7
  .byte 97 # 'a' # ClassDecl at 4.7
  .byte 105 # 'i' # ClassDecl at 4.7
  .byte 110 # 'n' # ClassDecl at 4.7
4312:CLASS_Main: # ClassDecl at 4.7
  .word mth_Object_hashCode # MethodDeclNonVoid at 0.0
  .word mth_Object_equals # MethodDeclNonVoid at 0.0
  .word mth_Object_toString # MethodDeclNonVoid at 0.0
  .word mth_Main_main # MethodDeclVoid at 7.17
  .word mth_Main_fib # MethodDeclNonVoid at 28.16
4332:END_CLASS_Main: # ClassDecl at 4.7
4332:dataArrayVTableStart: # IntegerType at 0.0
4332:END_CLASS_Object: # ClassDecl at 0.0
  .byte 84 # StringLiteral at 19.15
  .byte 104 # StringLiteral at 19.15
  .byte 101 # StringLiteral at 19.15
  .byte 32 # StringLiteral at 19.15
  .byte 102 # StringLiteral at 19.15
  .byte 105 # StringLiteral at 19.15
  .byte 98 # StringLiteral at 19.15
  .byte 111 # StringLiteral at 19.15
  .byte 110 # StringLiteral at 19.15
  .byte 97 # StringLiteral at 19.15
  .byte 99 # StringLiteral at 19.15
  .byte 99 # StringLiteral at 19.15
  .byte 105 # StringLiteral at 19.15
  .byte 32 # StringLiteral at 19.15
  .byte 110 # StringLiteral at 19.15
  .byte 117 # StringLiteral at 19.15
  .byte 109 # StringLiteral at 19.15
  .byte 98 # StringLiteral at 19.15
  .byte 101 # StringLiteral at 19.15
  .byte 114 # StringLiteral at 19.15
  .byte 32 # StringLiteral at 19.15
  .byte 111 # StringLiteral at 19.15
  .byte 102 # StringLiteral at 19.15
  .byte 32 # StringLiteral at 19.15
  .word CLASS_String # StringLiteral at 19.15
  .word 7 # StringLiteral at 19.15
  .word -24 # StringLiteral at 19.15
4368:strLit_16: # StringLiteral at 19.15
  .byte 32 # StringLiteral at 21.15
  .byte 105 # StringLiteral at 21.15
  .byte 115 # StringLiteral at 21.15
  .byte 32 # StringLiteral at 21.15
  .word CLASS_String # StringLiteral at 21.15
  .word 2 # StringLiteral at 21.15
  .word -4 # StringLiteral at 21.15
4384:strLit_24: # StringLiteral at 21.15
  .byte 46 # StringLiteral at 23.15
  .byte 10 # StringLiteral at 23.15
  .byte 0 # StringLiteral at 23.15
  .byte 0 # StringLiteral at 23.15
  .word CLASS_String # StringLiteral at 23.15
  .word 2 # StringLiteral at 23.15
  .word -2 # StringLiteral at 23.15
4400:strLit_32: # StringLiteral at 23.15
# begin: Program at 4.1; stackHeight = 0
.text
.globl main
0:main:
0#  jal vm_init
# begin: CallStatement at 0.0; stackHeight = 0
# begin: Call at 0.0; stackHeight = 0
# begin: NewObject at 0.0; stackHeight = 0
4#li $s6, 2
8#li $s7, 1
12#jal newObject
16#la $t0, CLASS_Main
20#sw $t0, -12($s7)
# end: NewObject at 0.0; stackHeight = 4
24#lw $t0, 0($sp)
28#sw $s2, 0($sp)
32#move $s2, $t0
36#beq $s2, $0, nullPtrException
40#lw $t0, -12($s2)
44#lw $t0, 12($t0)
48#jalr $t0
52#addu $sp, $sp, 0
56#lw $s2, ($sp)
60#addu $sp, $sp, 4
64#subu $sp, $sp, 4
68#sw $t0, ($sp)
# end: Call at 0.0; stackHeight = 4
72#lw $t0, ($sp)
76#addu $sp, $sp, 4
# end: CallStatement at 0.0; stackHeight = 0
80#  li $v0, 10
84#  syscall
# begin: MethodDeclVoid at 7.17; stackHeight = 0
.globl mth_Main_main
88:mth_Main_main:
88#subu $sp, $sp, 4
92#sw $ra, ($sp)
# begin: LocalVarDecl at 10.6; stackHeight = 0
# begin: NewObject at 10.12; stackHeight = 0
96#li $s6, 2
100#li $s7, 1
104#jal newObject
108#la $t0, CLASS_Lib
112#sw $t0, -12($s7)
# end: NewObject at 10.12; stackHeight = 4
116#lw $0, ($sp) #**lib
# end: LocalVarDecl at 10.6; stackHeight = 4
# begin: LocalVarDecl at 13.6; stackHeight = 4
# begin: IntegerLiteral at 13.12; stackHeight = 4
120#li $t0, 14
124#subu $sp, $sp, 8
128#sw $s5, 4($sp)
132#sw $t0, ($sp)
# end: IntegerLiteral at 13.12; stackHeight = 12
136#lw $0, ($sp) #**val
# end: LocalVarDecl at 13.6; stackHeight = 12
# begin: LocalVarDecl at 16.6; stackHeight = 12
# begin: Call at 16.15; stackHeight = 12
140#subu $sp, $sp, 4
144#sw $s2, ($sp)
# begin: IdentifierExp at 16.19; stackHeight = 16
148#lw $t0, 4($sp) #**LocalVar
152#subu $sp, $sp, 8
156#sw $s5, 4($sp)
160#sw $t0, ($sp)
# end: IdentifierExp at 16.19; stackHeight = 24
164#lw $t0, 8($sp)
168#sw $s2, 8($sp)
172#move $s2, $t0
176#beq $s2, $0, nullPtrException
180#lw $t0, -12($s2)
184#lw $t0, 16($t0)
188#jalr $t0
192#addu $sp, $sp, 8
196#lw $s2, ($sp)
200#addu $sp, $sp, 4
204#subu $sp, $sp, 8
208#sw $s5, 4($sp)
212#sw $t0, ($sp)
# end: Call at 16.15; stackHeight = 20
216#lw $0, ($sp) #**result
# end: LocalVarDecl at 16.6; stackHeight = 20
# begin: CallStatement at 19.6; stackHeight = 20
# begin: Call at 19.6; stackHeight = 20
# begin: IdentifierExp at 19.2; stackHeight = 20
220#lw $t0, 16($sp) #**LocalVar
224#subu $sp, $sp, 4
228#sw $t0, ($sp)
# end: IdentifierExp at 19.2; stackHeight = 24
# begin: StringLiteral at 19.15; stackHeight = 24
232#li $t0, strLit_16
236#subu $sp, $sp, 4
240#sw $t0, ($sp)
# end: StringLiteral at 19.15; stackHeight = 28
244#lw $t0, 4($sp)
248#sw $s2, 4($sp)
252#move $s2, $t0
256#beq $s2, $0, nullPtrException
260#lw $t0, -12($s2)
264#lw $t0, 24($t0)
268#jalr $t0
272#addu $sp, $sp, 4
276#lw $s2, ($sp)
280#addu $sp, $sp, 4
284#subu $sp, $sp, 4
288#sw $t0, ($sp)
# end: Call at 19.6; stackHeight = 24
292#lw $t0, ($sp)
296#addu $sp, $sp, 4
# end: CallStatement at 19.6; stackHeight = 20
# begin: CallStatement at 20.6; stackHeight = 20
# begin: Call at 20.6; stackHeight = 20
# begin: IdentifierExp at 20.2; stackHeight = 20
300#lw $t0, 16($sp) #**LocalVar
304#subu $sp, $sp, 4
308#sw $t0, ($sp)
# end: IdentifierExp at 20.2; stackHeight = 24
# begin: IdentifierExp at 20.15; stackHeight = 24
312#lw $t0, 12($sp) #**LocalVar
316#subu $sp, $sp, 8
320#sw $s5, 4($sp)
324#sw $t0, ($sp)
# end: IdentifierExp at 20.15; stackHeight = 32
328#lw $t0, 8($sp)
332#sw $s2, 8($sp)
336#move $s2, $t0
340#beq $s2, $0, nullPtrException
344#lw $t0, -12($s2)
348#lw $t0, 32($t0)
352#jalr $t0
356#addu $sp, $sp, 8
360#lw $s2, ($sp)
364#addu $sp, $sp, 4
368#subu $sp, $sp, 4
372#sw $t0, ($sp)
# end: Call at 20.6; stackHeight = 24
376#lw $t0, ($sp)
380#addu $sp, $sp, 4
# end: CallStatement at 20.6; stackHeight = 20
# begin: CallStatement at 21.6; stackHeight = 20
# begin: Call at 21.6; stackHeight = 20
# begin: IdentifierExp at 21.2; stackHeight = 20
384#lw $t0, 16($sp) #**LocalVar
388#subu $sp, $sp, 4
392#sw $t0, ($sp)
# end: IdentifierExp at 21.2; stackHeight = 24
# begin: StringLiteral at 21.15; stackHeight = 24
396#li $t0, strLit_24
400#subu $sp, $sp, 4
404#sw $t0, ($sp)
# end: StringLiteral at 21.15; stackHeight = 28
408#lw $t0, 4($sp)
412#sw $s2, 4($sp)
416#move $s2, $t0
420#beq $s2, $0, nullPtrException
424#lw $t0, -12($s2)
428#lw $t0, 24($t0)
432#jalr $t0
436#addu $sp, $sp, 4
440#lw $s2, ($sp)
444#addu $sp, $sp, 4
448#subu $sp, $sp, 4
452#sw $t0, ($sp)
# end: Call at 21.6; stackHeight = 24
456#lw $t0, ($sp)
460#addu $sp, $sp, 4
# end: CallStatement at 21.6; stackHeight = 20
# begin: CallStatement at 22.6; stackHeight = 20
# begin: Call at 22.6; stackHeight = 20
# begin: IdentifierExp at 22.2; stackHeight = 20
464#lw $t0, 16($sp) #**LocalVar
468#subu $sp, $sp, 4
472#sw $t0, ($sp)
# end: IdentifierExp at 22.2; stackHeight = 24
# begin: IdentifierExp at 22.15; stackHeight = 24
476#lw $t0, 4($sp) #**LocalVar
480#subu $sp, $sp, 8
484#sw $s5, 4($sp)
488#sw $t0, ($sp)
# end: IdentifierExp at 22.15; stackHeight = 32
492#lw $t0, 8($sp)
496#sw $s2, 8($sp)
500#move $s2, $t0
504#beq $s2, $0, nullPtrException
508#lw $t0, -12($s2)
512#lw $t0, 32($t0)
516#jalr $t0
520#addu $sp, $sp, 8
524#lw $s2, ($sp)
528#addu $sp, $sp, 4
532#subu $sp, $sp, 4
536#sw $t0, ($sp)
# end: Call at 22.6; stackHeight = 24
540#lw $t0, ($sp)
544#addu $sp, $sp, 4
# end: CallStatement at 22.6; stackHeight = 20
# begin: CallStatement at 23.6; stackHeight = 20
# begin: Call at 23.6; stackHeight = 20
# begin: IdentifierExp at 23.2; stackHeight = 20
548#lw $t0, 16($sp) #**LocalVar
552#subu $sp, $sp, 4
556#sw $t0, ($sp)
# end: IdentifierExp at 23.2; stackHeight = 24
# begin: StringLiteral at 23.15; stackHeight = 24
560#li $t0, strLit_32
564#subu $sp, $sp, 4
568#sw $t0, ($sp)
# end: StringLiteral at 23.15; stackHeight = 28
572#lw $t0, 4($sp)
576#sw $s2, 4($sp)
580#move $s2, $t0
584#beq $s2, $0, nullPtrException
588#lw $t0, -12($s2)
592#lw $t0, 24($t0)
596#jalr $t0
600#addu $sp, $sp, 4
604#lw $s2, ($sp)
608#addu $sp, $sp, 4
612#subu $sp, $sp, 4
616#sw $t0, ($sp)
# end: Call at 23.6; stackHeight = 24
620#lw $t0, ($sp)
624#addu $sp, $sp, 4
# end: CallStatement at 23.6; stackHeight = 20
628#addu $sp, $sp, 20
632#lw $ra, ($sp)
636#addu $sp, $sp, 4
640#jr $ra
# end: MethodDeclVoid at 7.17; stackHeight = -4
.globl mth_Main_fib
644:mth_Main_fib:
644#subu $sp, $sp, 4
648#sw $ra, ($sp)
# begin: LocalVarDecl at 29.6; stackHeight = 0
# begin: IntegerLiteral at 29.13; stackHeight = 0
652#li $t0, 1
656#subu $sp, $sp, 8
660#sw $s5, 4($sp)
664#sw $t0, ($sp)
# end: IntegerLiteral at 29.13; stackHeight = 8
668#lw $0, ($sp) #**prev
# end: LocalVarDecl at 29.6; stackHeight = 8
# begin: LocalVarDecl at 30.6; stackHeight = 8
# begin: IntegerLiteral at 30.16; stackHeight = 8
672#li $t0, 0
676#subu $sp, $sp, 8
680#sw $s5, 4($sp)
684#sw $t0, ($sp)
# end: IntegerLiteral at 30.16; stackHeight = 16
688#lw $0, ($sp) #**current
# end: LocalVarDecl at 30.6; stackHeight = 16
# begin: Block at 31.2; stackHeight = 16
# begin: LocalVarDecl at 31.11; stackHeight = 16
# begin: IntegerLiteral at 31.15; stackHeight = 16
692#li $t0, 0
696#subu $sp, $sp, 8
700#sw $s5, 4($sp)
704#sw $t0, ($sp)
# end: IntegerLiteral at 31.15; stackHeight = 24
708#lw $0, ($sp) #**i
# end: LocalVarDecl at 31.11; stackHeight = 24
# begin: BreakTarget at 31.2; stackHeight = 24
712:while_cond_73:
# begin: LessThan at 31.20; stackHeight = 24
# begin: IdentifierExp at 31.18; stackHeight = 24
712#lw $t0, 0($sp) #**LocalVar
716#subu $sp, $sp, 8
720#sw $s5, 4($sp)
724#sw $t0, ($sp)
# end: IdentifierExp at 31.18; stackHeight = 32
# begin: IdentifierExp at 31.22; stackHeight = 32
728#lw $t0, 36($sp) #**LocalVar
732#subu $sp, $sp, 8
736#sw $s5, 4($sp)
740#sw $t0, ($sp)
# end: IdentifierExp at 31.22; stackHeight = 40
744#lw $t2, ($sp)
748#addu $sp, $sp, 8
752#lw $t1, ($sp)
756#addu $sp, $sp, 8
760#slt $t0, $t1, $t2
764#subu $sp, $sp, 4
768#sw $t0, ($sp)
# end: LessThan at 31.20; stackHeight = 28
772#lw $t0, ($sp)
776#addu $sp, $sp, 4
780#beq $t0, $0, break_target_73
# begin: Block at 31.2; stackHeight = 24
# begin: Block at 31.30; stackHeight = 24
# begin: LocalVarDecl at 32.10; stackHeight = 24
# begin: IdentifierExp at 32.17; stackHeight = 24
784#lw $t0, 8($sp) #**LocalVar
788#subu $sp, $sp, 8
792#sw $s5, 4($sp)
796#sw $t0, ($sp)
# end: IdentifierExp at 32.17; stackHeight = 32
800#lw $0, ($sp) #**temp
# end: LocalVarDecl at 32.10; stackHeight = 32
# begin: Assign at 33.14; stackHeight = 32
# begin: Plus at 33.24; stackHeight = 32
# begin: IdentifierExp at 33.16; stackHeight = 32
804#lw $t0, 16($sp) #**LocalVar
808#subu $sp, $sp, 8
812#sw $s5, 4($sp)
816#sw $t0, ($sp)
# end: IdentifierExp at 33.16; stackHeight = 40
# begin: IdentifierExp at 33.26; stackHeight = 40
820#lw $t0, 32($sp) #**LocalVar
824#subu $sp, $sp, 8
828#sw $s5, 4($sp)
832#sw $t0, ($sp)
# end: IdentifierExp at 33.26; stackHeight = 48
836#lw $t2, ($sp)
840#addu $sp, $sp, 8
844#lw $t1, ($sp)
848#addu $sp, $sp, 8
852#addu $t0, $t1, $t2
856#subu $sp, $sp, 8
860#sw $s5, 4($sp)
864#sw $t0, ($sp)
# end: Plus at 33.24; stackHeight = 40
868#lw $t0, ($sp)
872#addu $sp, $sp, 8
876#sw $t0, 16($sp)
# end: Assign at 33.14; stackHeight = 32
# begin: Assign at 34.11; stackHeight = 32
# begin: IdentifierExp at 34.13; stackHeight = 32
880#lw $t0, 0($sp) #**LocalVar
884#subu $sp, $sp, 8
888#sw $s5, 4($sp)
892#sw $t0, ($sp)
# end: IdentifierExp at 34.13; stackHeight = 40
896#lw $t0, ($sp)
900#addu $sp, $sp, 8
904#sw $t0, 24($sp)
# end: Assign at 34.11; stackHeight = 32
908#addu $sp, $sp, 8
# end: Block at 31.30; stackHeight = 24
# begin: Assign at 31.25; stackHeight = 24
# begin: Plus at 31.26; stackHeight = 24
# begin: IdentifierExp at 31.25; stackHeight = 24
912#lw $t0, 0($sp) #**LocalVar
916#subu $sp, $sp, 8
920#sw $s5, 4($sp)
924#sw $t0, ($sp)
# end: IdentifierExp at 31.25; stackHeight = 32
# begin: IntegerLiteral at 31.26; stackHeight = 32
928#li $t0, 1
932#subu $sp, $sp, 8
936#sw $s5, 4($sp)
940#sw $t0, ($sp)
# end: IntegerLiteral at 31.26; stackHeight = 40
944#lw $t2, ($sp)
948#addu $sp, $sp, 8
952#lw $t1, ($sp)
956#addu $sp, $sp, 8
960#addu $t0, $t1, $t2
964#subu $sp, $sp, 8
968#sw $s5, 4($sp)
972#sw $t0, ($sp)
# end: Plus at 31.26; stackHeight = 32
976#lw $t0, ($sp)
980#addu $sp, $sp, 8
984#sw $t0, 0($sp)
# end: Assign at 31.25; stackHeight = 24
988#addu $sp, $sp, 0
# end: Block at 31.2; stackHeight = 24
992#j while_cond_73
996:break_target_73:
# end: BreakTarget at 31.2; stackHeight = 24
996#addu $sp, $sp, 8
# end: Block at 31.2; stackHeight = 16
# begin: IdentifierExp at 36.9; stackHeight = 16
1000#lw $t0, 0($sp) #**LocalVar
1004#subu $sp, $sp, 8
1008#sw $s5, 4($sp)
1012#sw $t0, ($sp)
# end: IdentifierExp at 36.9; stackHeight = 24
1016#lw $t0, ($sp)
1020#addu $sp, $sp, 8
1024#addu $sp, $sp, 16
1028#lw $ra, ($sp)
1032#addu $sp, $sp, 4
1036#jr $ra
# end: Program at 4.1; stackHeight = -4
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
1040:mth_Object_hashCode:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
1040#    lw $t0,-8($s2) # get word with hash bits from this
1044#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
1048#    addu $t0,$t1

1052#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
1056:mth_Object_equals:
1056#    move $t0,$s2 # first pointer (this)
1060#    lw $t1,($sp) # second pointer
1064#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
1065##...subu $at,$t0,$t1
1069##...sltiu $t0,$at,1

1072#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
1076:mth_String_hashCode:
1076#    move $t0,$s2 # put this pointer in t0

1080#    li $t2,0 # current hash value
1084#    lw $t1,-4($t0) # negative of number of characters in string
1088#    beq $t1,$zero,done_hashCode_String # if zero length => done
1092#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
1096#    and $t0,0xfffffffc # adjust for padding
1097##...lui $at,65535
1101##...ori $at,$at,0xfffc
1105##...and $t0,$t0,$at
1108#    subu $t1,$t0,$t1 # address at end of string +12
1112:loop_hashCode_String:    
1112#    sll $t4,$t2,5 # multiply by 32
1116#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
1120#    lbu $t3,-12($t0) # current character
1124#    addu $t2,$t3 # add in current character
1128#    addu $t0,1 # go to next character's address
1132#    bne $t0,$t1,loop_hashCode_String # loop back if more
1136:done_hashCode_String:    

    # push int on stack
1136#    move $t0, $t2 # put $t2 in return slot
1140#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
1144:mth_Object_toString:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
1144#    subu $sp,4
1148#    sw $ra,($sp)
    
    # get hash value
1152#    lw $t2,-12($s2) # vtable address
1156#    lw $t0,($t2) # method address
1160#    jalr $t0
1164#    subu $sp,8 # push result (int in $t0) on stack
1168#    sw $s5,4($sp)
1172#    sw $t0,($sp)

    # compute print-size of hash value
1176#    li $t0, 1
1180#    lw $t1, ($sp)
1184#    srl $t1,$t1,4
1188#    beq $t1,$zero,toString_skip_loop
1192:toString_loop_top:
1192#    addu $t0,1
1196#    srl $t1,$t1,4
1200#    bne $t1,$zero,toString_loop_top
1204:toString_skip_loop:
1204#    subu $sp,8
1208#    sw $s5,4($sp)
1212#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
1216#    lw $t2,-12($s2) # vtable address
1220#    move $t0,$t2
1224:toString_checkTagBit:
1224#    subu $t0,1
1228#    lb $t1,($t0)
1232#    and $t1,0x80
1233##...lui $at,0
1237##...ori $at,$at,0x80
1241##...and $t1,$t1,$at
1244#    beq $t1,$zero,toString_checkTagBit
1248#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
1252#    subu $sp,8
1256#    sw $t1,($sp)
1260#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
1264#    move $s7,$zero # -4 offset header word
1268#    lw $t4,8($sp) # bytes for hash value
1272#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
1276#    addu $t4,8
1280#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
1284#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

1288#    li $t0,CLASS_String # store string vtable address into ...
1292#    sw $t0,-12($s7) # ... -12 offset in object header
1296#    lw $t2,-12($s2) # our object's vtable address
1300#    lw $t5,4($sp) # number characters in prefix
1304#    lw $t1,12($sp) # number of characters in hashcode
1308#    addu $t3,$t1,$t5 # number of characters in string ...
1312#    addu $t3,1 # ... including the '@' character
1316#    subu $t0,$zero,$t3 # store negated length into ...
1320#    sw $t0,-4($s7) # ... -4 offset header word
1324#    lw $t0,-8($s7) # get -8 offset header-word
1328#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
1329##...slt $at,$t0,$zero
1333##...beq $at,$zero,1352
1336#    and $t0,0xff # mask off object's hash value ...
1337##...lui $at,0
1341##...ori $at,$at,0xff
1345##...and $t0,$t0,$at
1348#    sw $t0,-8($s7) # ... and store back into -8 header-word
1352:skipMask_toString:
1352#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
1356#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
1360#    subu $t4,$t2,$t5
1364:toString_copyLoop:    
1364#    lb $t3,($t4)
1368#    and $t3,0x7f # mask off sign bit (in case first word)
1369##...lui $at,0
1373##...ori $at,$at,0x7f
1377##...and $t3,$t3,$at
1380#    sb $t3,-8($t0)
1384#    addu $t4,1
1388#    addu $t0,1
1392#    blt $t4,$t2,toString_copyLoop
1393##...slt $at,$t4,$t2
1397##...bne $at,$zero,1364

    # copy the '@' sign
1400#    li $t3,'@'
1404#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
1408#    addu $t2,$t0,$t1 # address of last byte in string to store +8
1412#    lw $t3,20($sp) # hash code to print
1416:toString_hashCopyLoop:
1416#    and $t4,$t3,0xf
1420#    srl $t3,4
1424#    lb $t4,hexChars($t4)
1428#    sb $t4,-8($t2)
1432#    subu $t2,$t2,1
1436#    bgt $t2,$t0,toString_hashCopyLoop
1437##...slt $at,$t0,$t2
1441##...bne $at,$zero,1416

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
1444#    addu $sp,28
1448#    lw $ra,($sp)
1452#    addu $sp,4
1456#    move $t0, $s7 # return string we created
1460#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
1464:mth_String_equals:
    
    # quick test for obvious false
1464#    lw $t0,($sp) # second parameter
1468#    beq $t0,$zero,goEsFalse # go return false if null
1472#    lw $t1,-12($t0) # vtable pointer
1476#    la $t2,CLASS_String # string vtable pointer
1480#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
1484#    subu $sp,12
1488#    sw $ra,8($sp)
1492#    sw $s2,4($sp)
1496#    sw $t0,($sp)
1500#    jal mth_String_compareTo
1504#    lw $s2, 4($sp) #restore this pointer and pop parameters
1508#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
1512#    seq $t0,$t0,$zero
1513##...subu $at,$t0,$zero
1517##...sltiu $t0,$at,1

    # pop return address and return $t0
1520#    lw $ra,($sp)
1524#    addu $sp,4

1528#    jr $ra # return
    
1532:goEsFalse:
    # put 0 in $t0 and return
1532#    li $t0, 0
1536#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
1540:mth_String_toString:
    #we are a string, so return this.
1540#    move $t0, $s2
1544#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
1548:mth_Lib_readLine:
    
1548#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
1552#    sw $ra, 4($sp) # save $ra
1556#    move $t1,$sp # save "original" sp

1560:  doRead:

    # read the character
1560#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
1564#    subu $t2,$v0,13
1568#    bne $t2,$zero,notReturnRL
1572#    jal readLogicalChar
1576#    subu $t2,$v0,10 # check for newline
1580#    beq $t2,$zero,foundNewLine
1584#    sw $v0,lastCharRead # push back character into queue
1588#    j foundNewLine # go process the line

1592:  notReturnRL:

    # at this point, $v0 has our character

1592#    subu $t0,$v0,10
1596#    beq $t0,$zero,foundNewLine
1600#    blt $v0,$zero,foundEof
1601##...slt $at,$v0,$zero
1605##...bne $at,$zero,1620

    # we have a character, so push it onto stack
1608#    subu $sp,$sp,4
1612#    sw $v0,($sp)

    # loop back up to get next character
1616#    j doRead

1620:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
1620#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
1624#    li $s7, 0 # return-value: null
1628#    j rlReturn # return

1632:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
1632#    subu $t0,$t1,$sp # number of chars we read (times 4)
1636#    srl $s6,$t0,2 # number of words on stack with chars
1640#    subu $sp,$sp,4 # push char-count ...
1644#    sw $s6,($sp) # ... onto stack
1648#    addu $t2,$t0,5 # GC tag, incl. for count-word
1652#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
1656#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
1660#    srl $s6,$s6,2  # data words in object
1664#    move $s7,$zero # object words in object
1668#    jal newObject  # allocate space
    
    # store header words
1672#    la $t0,CLASS_String
1676#    sw $t0,-12($s7)# store class tag (String) into object
1680#    lw $t2,4($sp) # char-count
1684#    sll $t1,$t2,2 # 4 times number of chars
1688#    subu $t2,$zero,$t2 # negative of char-count
1692#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
1696#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
1700#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
1701##...slt $at,$t0,$zero
1705##...beq $at,$zero,1724
1708#    and $t0,0xff # mask off object's hash value ...
1709##...lui $at,0
1713##...ori $at,$at,0xff
1717##...and $t0,$t0,$at
1720#    sw $t0,-8($s7) # ... and store back into -8 header-word
1724:  skipMask_readLine:

1724#    sll $t0,$t0,2 # data bytes in object
1728#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
1732#    subu $t0,$t0,$t2 # place to store last character (plus 9)
1736#    addu $sp,$sp,8 # pop redundant object-pointer and count
1740#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

1744#    beq $sp,$t1,rlReturn
1748:  charCopyLoop:
1748#    lw $t2,($sp)
1752#    sb $t2,-9($t0)
1756#    addu $sp,$sp,4
1760#    subu $t0,$t0,1
1764#    bne $sp,$t1,charCopyLoop
1768:  rlReturn:

1768#    lw $ra, 4($sp)
1772#    addu $sp, 8   # pop $ra and pointless word off stack
1776#    move $t0, $s7 # return string we allocated in $s7
1780#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1784:mth_Lib_readInt:

    # save $ra by pushing onto stack
1784#    subu $sp,$sp,4
1788#    sw $ra,($sp)

1792:  riSkipWhiteLoop:
    # read a character
1792#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
1796#    subu $t0,$v0,32
1800#    bgt $t0,$zero,nonWhite
1801##...slt $at,$zero,$t0
1805##...bne $at,$zero,1848
1808#    beq $t0,$zero,riSkipWhiteLoop # start over if space
1812#    subu $t0,$v0,10
1816#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
1820#    subu $t0,$v0,9
1824#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
1828#    subu $t0,$v0,13
1832#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1836#    subu $t0,$v0,12
1840#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1844#    j badIntegerFormat # illegal integer char: abort program

1848:  nonWhite:
1848#    subu $t0,$v0,'-'
1852#    li $t4,1 # final multiplier
1856#    bne $t0,$zero,helpReadInt # go read
1860#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1864#    jal readLogicalChar

1868:  helpReadInt:
1868#    li $t2,10
1872#    subu $t1,$v0,'0' # convert digit to 0-9 value
1876#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1877##...sltu $at,$t1,$t2
1881##...beq $at,$zero,4164
1884#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1888:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1888#    jal readLogicalChar # read next character
1892#    subu $t1,$v0,'0' # convert digit to 0-9 value
1896#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1897##...sltu $at,$t1,$t2
1901##...beq $at,$zero,1920

1904#    mul $t3,$t3,$t2 # multiply old value by 10
1905##...mult $t3,$t2
1909##...mflo $t3
1912#    addu $t3,$t3,$t1 # add in value of new digit
1916#    j digitLoop

1920:  doneDigitLoop:

    # "push back" unused character into queue
1920#    sw $v0,lastCharRead

    # restore return address
1924#    lw $ra,($sp)
1928#    addu $sp, 4

    # multiply by sign and put return value in $t0
1932#    mult $t3,$t4 # multiply to account for poss. minus sign
1936#    mflo $t3
1940#    move $t0, $t3
    
    # return
1944#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1948:mth_Lib_readChar:

    # save $ra by pushing onto stack
1948#    subu $sp,$sp,4
1952#    sw $ra,($sp)

      # read the character
1956#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1960#    lw $ra,($sp)
1964#    addu $sp, 4
1968#    move $t0, $v0

    # return
1972#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1976:mth_Lib_printStr:
    # check for null, printing "(null)", if so
1976#    lw $t0,($sp)
1980#    bne $t0,$zero,psNotNull

    # print "(null)"
1984#    la $a0,nullStr
1988#    li $v0,4
1992#    syscall
1996#    j donePrintStr
    
2000:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
2000#    subu $t0,$t0,8
2004#    lw $t1,($t0) # number of data words, but needs modification
2008#    blt $t1,$zero,skipPrintStr1
2009##...slt $at,$t1,$zero
2013##...bne $at,$zero,2032
2016#    and $t1,0xff # mask out all but low byte
2017##...lui $at,0
2021##...ori $at,$at,0xff
2025##...and $t1,$t1,$at
2028#    b skipPrintStr2
2032:skipPrintStr1:
2032#    and $t1,0x7ffffff
2033##...lui $at,2047
2037##...ori $at,$at,0xffff
2041##...and $t1,$t1,$at
2044:skipPrintStr2:    
2044#    sll $t1,$t1,2
2048#    subu $t1,$t0,$t1 # first word in string
2052#    lw $t0,4($t0) # negative of string-length
2056#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
2060#    beq $t0,$t1,donePrintStr
2064:  psLoop:
2064#    lb $a0,($t1) # next byte
2068#    li $v0,11 # code for printing char
2072#        syscall # print the char
2076#    addu $t1,$t1,1 # go to next char
2080#    blt $t1,$t0,psLoop
2081##...slt $at,$t1,$t0
2085##...bne $at,$zero,2064

2088:  donePrintStr:
2088#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
2092:mth_Lib_printInt:
2092#    lw $a0,($sp)

    # print it
2096#    li $v0,1 # code for print-int
2100#    syscall

    # return
2104#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
2108:mth_Lib_printBool:
    # pop value off stack, along with 'this'
2108#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
2112#    la $a0,falseString
2116#    beq $t0,$zero,skipPB
2120#    la $a0,trueString
2124:  skipPB:
2124#    li $v0,4 # code for print-string
2128#    syscall

    # return
2132#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
2136:mth_String_substring:

    # save $ra by pushing onto stack
2136#    subu $sp,$sp,4
2140#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
2144#    move $t0,$s2
2148#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
2152#    lw $t0,-4($t0) # negative of string length
2156#    subu $t0,$zero,$t0 # string length
2160#    lw $t1,4($sp) # k
2164#    lw $t2,12($sp) # n
2168#    bgt $zero,$t2,strIndexOutOfBounds
2169##...slt $at,$t2,$zero
2173##...bne $at,$zero,4132
2176#    bgt $t2,$t1,strIndexOutOfBounds
2177##...slt $at,$t1,$t2
2181##...bne $at,$zero,4132
2184#    bgt $t1,$t0,strIndexOutOfBounds
2185##...slt $at,$t0,$t1
2189##...bne $at,$zero,4132

    # allocate memory
2192#    subu $s6,$t1,$t2 # # chars in target-string
2196#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
2200#    srl $s6,$s6,2 # convert bytes-count to word-count
2204#    move $s7,$zero # (no object-bytes in string)
2208#    jal newObject

    # store "String" tag in object-type field
2212#    la $t0,CLASS_String
2216#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
2220#    lw $t4,-8($s7) # data words in source string
2224#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
2225##...slt $at,$t4,$zero
2229##...beq $at,$zero,2248
2232#    and $t4,0xff # mask off object's hash value ...
2233##...lui $at,0
2237##...ori $at,$at,0xff
2241##...and $t4,$t4,$at
2244#    sw $t4,-8($s7) # ... and store back into -8 header-word
2248:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
2248#    lw $t1,8($sp) # k
2252#    lw $t2,16($sp) # n
2256#    subu $t0,$t2,$t1 # value to store
2260#    sw $t0,-4($s7) # store value
    
2264#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
2268#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
2272#    lw $t4,-8($t3) # data words in source string
2276#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
2280#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
2284#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
2288#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
2292#    lw $t2,-8($s7)   # data words in target string
2296#    sll $t2,$t2,2    # data bytes in target string
2300#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
2304:  subCopyLoopzz:
2304#    lb $t4,-8($t3)
2308#    sb $t4,-8($t2)
2312#    addu $t2,$t2,1
2316#    addu $t3,$t3,1
2320#    blt $t3,$t1,subCopyLoopzz
2321##...slt $at,$t3,$t1
2325##...bne $at,$zero,2304
    
2328:  doneSubCopyzz:
    # restore return address, store return value, pop stack
2328#    lw $ra,4($sp) # restore $ra
2332#    addu $sp,$sp,8 # pop ra, and allocated newObject
2336#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
2340#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
2344:mth_String_length:

    # the length is the negative of 4($s2)
2344#    lw $t0,-4($s2) # -length
2348#    subu $t0,$zero,$t0

    # return
2352#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
2356:mth_String_concat:
    # save $ra by pushing onto stack
2356#    subu $sp,$sp,4
2360#    sw $ra, ($sp)

    # get string pointers and check parameter for null
2364#    lw $t0,4($sp)
2368#    beq $t0,$zero,nullPtrException
2372#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
2376#    lw $t0,-4($t0) # negative size of second object
2380#    lw $t1,-4($t1) # negative size of first object
2384#    addu $s6,$t0,$t1 # sum of negative sizes
2388#    sra $s6,$s6,2 # negative word-size of char part
2392#    subu $s6,$zero,$s6 # word size of char part
2396#    addu $s6,$s6,1 # data word size, including v-table word
2400#    move $s7,$zero
2404#    jal newObject

    # store String vtable address in vtable address field
2408#    la $t0,CLASS_String
2412#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2416#    lw $t4,-8($s7) # # data words in source string
2420#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
2421##...slt $at,$t4,$zero
2425##...beq $at,$zero,2444
2428#    and $t4,0xff # mask off object's hash value ...
2429##...lui $at,0
2433##...ori $at,$at,0xff
2437##...and $t4,$t4,$at
2440#    sw $t4,-8($s7) # ... and store back into -8 header-word
2444:skipMask_concat:

2444#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
2448#    move $t1,$s2   # first object
2452#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
2456#    lw $t2,-4($t0) # negative length of second object
2460#    lw $t3,-4($t1) # negative length of first object
2464#    addu $t4,$t2,$t3 # sum of negative lengths
2468#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
2472#    lw $t4,-8($t1) # # data words in first string
2476#    sll $t4,$t4,2 # # data bytes in first string
2480#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
2484#    lw $t4,-8($s7) # # data words in new string
2488#    sll $t4,$t4,2 # # data bytes in new string
2492#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
2496#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
2500#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
2504:  concatLoop1zz:
2504#    lb $t4,-8($t1)
2508#    sb $t4,-8($s7)
2512#    addu $s7,$s7,1
2516#    addu $t1,$t1,1
2520#    blt $t1,$t3,concatLoop1zz
2521##...slt $at,$t1,$t3
2525##...bne $at,$zero,2504
2528:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
2528#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
2532#    lw $t4,-8($t0) # # data words in second string
2536#    sll $t4,$t4,2 # # data bytes in second string
2540#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
2544#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
2548:  concatLoop2zz:
2548#    lb $t4,-8($t1)
2552#    sb $t4,-8($s7)
2556#    addu $s7,$s7,1
2560#    addu $t1,$t1,1
2564#    blt $t1,$t3,concatLoop2zz
2565##...slt $at,$t1,$t3
2569##...bne $at,$zero,2548
2572:  doneConcatLoop2zz:
 
2572:  concatRtnzz:
    # return
2572#    move $t0, $t9 # put saved return string in $t0
2576#    lw $ra, 4($sp)
2580#    addu $sp, 8 # pop ra and allocated newObject
2584#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
2588:mth_String_charAt:
    # get string
2588#    move $t0,$s2

    # check that index is in bounds
2592#    lw $t1,-4($t0) # negative of # data words in string
2596#    subu $t3,$zero,$t1 # # chars in string
2600#    lw $t2,($sp) # index
2604#    bgeu $t2,$t3,strIndexOutOfBounds
2605##...sltu $at,$t2,$t3
2609##...beq $at,$zero,4132

    # access element
2612#    lw $t1,-8($t0) # # data words in object
2616#    sll $t1,$t1,2 # - byte-offset from end of chars
2620#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
2624#    addu $t0,$t0,$t1 # - address of our char, offset by 8
2628#    lb $t0,-8($t0) # our char

    # return
2632#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
2636:mth_Lib_intToString:

    # save return address on stack; allocate space for dummy GC tag
2636#    subu $sp,$sp,8
2640#    sw $ra,4($sp)

    # save current sp
2644#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
2648#    li $t3,10

    # get argument, negate if negative
2652#    lw $t1,8($sp)
2656#    bge $t1,$zero,itsNonNegLoop
2657##...slt $at,$t1,$zero
2661##...beq $at,$zero,2668
2664#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
2668:  itsNonNegLoop:
2668#    divu $t1,$t3
2672#    mflo $t1 # quotient
2676#    mfhi $t4 # remainder
2680#    addu $t4,$t4,'0' # turn remainder into digit
2684#    subu $sp,$sp,4
2688#    sw $t4,($sp) # push digit onto stack
2692#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
2696#    lw $t4,8($t0)
2700#    bge $t4,$zero,itsSkipNeg
2701##...slt $at,$t4,$zero
2705##...beq $at,$zero,2720
2708#    li $t4,'-'
2712#    subu $sp,$sp,4
2716#    sw $t4,($sp)
2720:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
2720#    subu $s6,$t0,$sp
2724#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
2728#    sw $t3,($t0)
2732#    srl $s6,$s6,2
2736#    subu $sp,$sp,4
2740#    sw $s6,($sp)
    
    # allocate memory
2744#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
2748#    srl $s6,$s6,2
2752#    move $s7,$zero # no "object" words in object
2756#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
2760#    lw $t0,4($sp)
2764#    addu $sp,$sp,8

    # store "String" tag into class field
2768#    subu $s7,$s7,8 # address of header-1 word
2772#    la $t1,CLASS_String
2776#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
2780#    lw $t4,($s7) # # data words in source string
2784#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
2785##...slt $at,$t4,$zero
2789##...beq $at,$zero,2808
2792#    and $t4,0xff # mask off object's hash value ...
2793##...lui $at,0
2797##...ori $at,$at,0xff
2801##...and $t4,$t4,$at
2804#    sw $t4,($s7) # ... and store back into -8 header-word
2808:skipMask_intToString:
    
    # store negative of char-count into header-2 word
2808#    subu $t0,$zero,$t0
2812#    sw $t0,4($s7)

2816#    lw $t1,($s7) # number of data words in string
2820#    sll $t1,$t1,2 # number data bytes in string
2824#    subu $t1,$s7,$t1 # first location to store chars in string
2828#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2832:  itsLoop:
2832#    lw $t2,($sp)
2836#    addu $sp,$sp,4
2840#    sb $t2,($t1)
2844#    addu $t1,$t1,1
2848#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2852#    lw $ra,4($sp)
2856#    addu $sp,$sp,8
2860#    addu $t0,$t0,15
2864#    and $t0,$t0,0xfffffffc
2865##...lui $at,65535
2869##...ori $at,$at,0xfffc
2873##...and $t0,$t0,$at
2876#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2880:mth_Lib_intToChar:
    # save return address
2880#    subu $sp,$sp,4
2884#    sw $ra,($sp)

    # allocate object
2888#    li $s6,2
2892#    move $s7,$zero
2896#    jal newObject

    # get n from string.
2900#    lw $t1,8($sp)

    # store "String" tag into class field
2904#    la $t0,CLASS_String
2908#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2912#    lw $t4,-8($s7) # # data words in source string
2916#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2917##...slt $at,$t4,$zero
2921##...beq $at,$zero,2940
2924#    and $t4,0xff # mask off object's hash value ...
2925##...lui $at,0
2929##...ori $at,$at,0xff
2933##...and $t4,$t4,$at
2936#    sw $t4,-8($s7) # ... and store back into -8 header-word
2940:  skipMask_intToChar:

    # store data in string
2940#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2944#    li $t0,-1
2948#    sw $t0,-4($s7)

    # we're returning $s7
2952#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2956#    lw $ra,4($sp)
2960#    addu $sp,$sp,8

    # return
2964#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2968:mth_String_compareTo:
    # get string pointers and check parameter for null
2968#    lw $t0,($sp) # second string
2972#    beq $t0,$zero,nullPtrException
2976#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2980#    lw $t2,-4($t0) # negative length of second string
2984#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2988#    slt $t4,$t3,$t2
2992#    slt $t5,$t2,$t3
2996#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
3000#    lw $t2,-8($t0) # # data words in second string
3004#    lw $t3,-8($t1) # # data words in first string
3008#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
3012#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
3016#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
3020#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
3024#    beq $zero,$t5,skipStrCmpLenzz
3028#    move $t2,$t3
3032:  skipStrCmpLenzz:
3032#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
3036:  cmpLoopzz:
3036#    lw $t3,-8($t1) # word from str2
3040#    lw $t4,-8($t0) # word from str1
3044#    bne $t3,$t4,cmpNotEqualzz
3048#    addu $t1,$t1,4
3052#    addu $t0,$t0,4
3056#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
3060#    j cmpHitLimitzz

3064:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
3064#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
3068#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
3072#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
3076:  cmpHitLimitzz:
3076#    move $t0, $t5 # store return value
3080#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
3084:readLogicalChar:

    # check if we already have a character
3084#    lw $v0,lastCharRead
3088#    li $t0,-2
3092#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
3096#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
3100#    jr $ra # return
    
3104:  doReadCh:
    # we need to actually read a character: read and return
3104#    li $v0,12 # use system call to read a character
3108#    syscall
3112#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
3116:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
3116#    blt $s7,$zero arraySizeOutOfBounds
3117##...slt $at,$s7,$zero
3121##...bne $at,$zero,4148

    # mark the fact that we have not yet GC'd for this allocation
3124#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

3128:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
3128#    addu $t0,$s6,$s7
3132#    sll $t0,$t0,2
3136#    addu $t0,$t0,8 # amount of memory needed
3140#    addu $t1,$t0,$s3
3144#    bgtu $t1,$s4,doGC
3145##...sltu $at,$s4,$t1
3149##...bne $at,$zero,3272

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
3152#    move $t2,$s3
3156:  zeroObjLoop:
3156#    sw $zero,($s3)
3160#    lw $zero,($s3)

3164#    addu $s3,4
3168#    bltu $s3,$t1,zeroObjLoop
3169##...sltu $at,$s3,$t1
3173##...bne $at,$zero,3156

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
3176#    sll $t0,$s7,2    # number of post-header bytes
3180#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
3184#    li $t2,255
3188#    bgeu $s6,$t2 firstHeaderLarge
3189##...sltu $at,$s6,$t2
3193##...beq $at,$zero,3240
3196#    lw $t1,prevHashConstant
3200#    addu $t1,0x12b68fd2
3201##...lui $at,4791
3205##...addiu $at,$at,-28718
3209##...addu $t1,$at,$t1
3212#    sw $t1,prevHashConstant # update hash constant
3216#    or $t1,0x80000000 # set top bit
3217##...lui $at,32768
3221##...or $t1,$at,$t1
3224#    and $t1,0xffffff00 # clear low bit
3225##...lui $at,65535
3229##...ori $at,$at,0xff00
3233##...and $t1,$t1,$at
3236#    or $s6,$t1
3240:firstHeaderLarge:
3240#    sw $s6,-8($t0) # store header-1 value
3244#    lw $zero,-8($t0)

    # store second header-value
3248#    sw $s7,-4($t0)   # second header-word
3252#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
3256#    move $s7,$t0
3260#    subu $sp,$sp,4
3264#    sw $t0,($sp)

3268#    jr $ra
    
3272:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
3272#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
3276#    lw $t5,gcCount
3280#    addu $t5,1
3284#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
3288#    subu $sp,$sp,4
3292#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
3296#    lw $t0,cur_seg
3300#    move $t7,$s4
3304#    la $t6,seg1_start
3305##...lui $t6,16
3309##...addiu $t6,$t6,4772
3312#    la $s3,seg0_start
3316#    la $s4,seg0_end
3317##...lui $s4,16
3321##...addiu $s4,$s4,4772
3324#    sw $zero,cur_seg
3328#    bne $t0,$zero,skipGc1
3332#    la $t6,seg0_start
3336#    la $s3,seg1_start
3337##...lui $s3,16
3341##...addiu $s3,$s3,4772
3344#    la $s4,seg1_end
3345##...lui $s4,32
3349##...addiu $s4,$s4,4772
3352#    sw $s5,cur_seg
3356:  skipGc1:
3356#    li $t5,0x80000000
3360#    lw $t0,stack_bottom
3364#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

3368:  gcStackCopyLoop:
3368#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
3372#    sll $t2,$t1,31 # zero iff low bit was zero
3376#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
3380#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
3384#    bleu $t1,$t6,gcTestIterDone1
3385##...sltu $at,$t6,$t1
3389##...beq $at,$zero,3552
3392#    bgtu $t1,$t7,gcTestIterDone1
3393##...sltu $at,$t7,$t1
3397##...bne $at,$zero,3552

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3400#    lw $t2,-8($t1) # possible forwarding tag
3404#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
3408#    lw $t2,-4($t1) # forwarding pointer: object's new address
3412#    sw $t2,4($t0) # update stack value
3416#    j gcTestIterDone1 # do next iteration

3420:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
3420#    bge $t2,$zero,gcSkip2
3421##...slt $at,$t2,$zero
3425##...beq $at,$zero,3440
3428#    and $t2,0xff #### mask off hash value
3429##...lui $at,0
3433##...ori $at,$at,0xff
3437##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

3440:  gcSkip2:
3440#    sll $t2,$t2,2
3444#    addu $t2,$t2,8
3448#    subu $t4,$t1,$t2 # address of first word of source
3452#    lw $t3,-4($t1) # number of object words (negative means zero)
3456#    bge $t3,$zero,gcH2Neg1
3457##...slt $at,$t3,$zero
3461##...beq $at,$zero,3468
3464#    move $t3,$zero
3468:  gcH2Neg1:
3468#    sll $t3,$t3,2
3472#    addu $t3,$t3,$t1 # address one word past last word of source

3476#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
3480#    lw $s0,($t4)
3484#    lw $s1,-8($t1)
3488#    sw $s1,($t4)
3492#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
3496:  gCinnerCopy1:
3496#    lw $s0,($t4)
3500#    sw $s0,($s3)
3504#    addu $t4,$t4,4
3508#    addu $s3,$s3,4
3512#    bltu $t4,$t3,gCinnerCopy1
3513##...sltu $at,$t4,$t3
3517##...bne $at,$zero,3496

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3520#    sw $zero,-8($t1) # 0 in first header-slot
3524#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
3528#    sw $t2,4($t0)
3532#    lw $zero,-8($t2)
3536#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
3540#    j gcTestIterDone1

3544:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
3544#    subu $t0,$t0,$t1
3548#    subu $t0,$t0,3

3552:  gcTestIterDone1:
3552#    bgeu $t0,$sp,gcStackCopyLoop
3553##...sltu $at,$t0,$sp
3557##...beq $at,$zero,3368

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
3560#    lw $t1,cur_seg
3564#    la $t0,seg0_start
3568#    beq $t1,$zero,gcSkip4
3572#    la $t0,seg1_start
3573##...lui $t0,16
3577##...addiu $t0,$t0,4772


3580:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
3580#    bgeu $t0,$s3,gcFinishUp
3581##...sltu $at,$t0,$s3
3585##...beq $at,$zero,3876

    ###### begin outer loop to copy all non-stack-direct objects ######

3588:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
3588#    lw $t1,($t0) # (what will be) first header word for current object
3592#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
3596#    bge $t2,$zero,gcSkip4a # are there hash bits?
3597##...slt $at,$t2,$zero
3601##...beq $at,$zero,3616
3604#    and $t2,0xff # mask off hash bits
3605##...lui $at,0
3609##...ori $at,$at,0xff
3613##...and $t2,$t2,$at
3616:gcSkip4a:    
3616#    sll $t2,$t2,2 # # data bytes
3620#    addu $t2,$t2,8 # to skip header words
3624#    addu $t2,$t0,$t2 # pointer to new object
3628#    lw $t3,-8($t2) # word to swap
3632#    sw $t1,-8($t2) # store header word
3636#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
3640#    lw $t1,-12($t2) # vtable pointer
3644#    li $t0,dataArrayVTableStart # address of vtable for first data array
3648#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
3649##...slt $at,$t1,$t0
3653##...bne $at,$zero,3672

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
3656:  skipToNextObj:
3656#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
3660#    sll $t1,$t1,2 # convert to byte-count
3664#    addu $t0,$t2,$t1 # skip over object
3668#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
3672:  gcNotDataArray:
3672#    lw $t3,-4($t2) # # object words (negative treated as zero)
3676#    bge $t3,$zero,gcH2Neg2
3677##...slt $at,$t3,$zero
3681##...beq $at,$zero,3688
3684#    move $t3,$zero
3688:  gcH2Neg2:
3688#    sll $t3,$t3,2 # # object bytes
3692#    move $t0,$t2 # address of first pointer in object
3696#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
3700#    beq $t0,$s1,gcTestIterDone2
    
3704:  gcNextPointerInObject:
    # get pointer from object
3704#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
3708#    bleu $t1,$t6,gcGoNextPointerSlot
3709##...sltu $at,$t6,$t1
3713##...beq $at,$zero,3856
3716#    bgtu $t1,$t7,gcGoNextPointerSlot
3717##...sltu $at,$t7,$t1
3721##...bne $at,$zero,3856

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3724#    lw $t8,-8($t1) # possible forwarding tag
3728#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
3732#    lw $t8,-4($t1) # forwarding pointer: object's new address
3736#    sw $t8,($t0) # update pointer in object
3740#    j gcGoNextPointerSlot # do next iteration

3744:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
3744#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
3745##...slt $at,$t8,$zero
3749##...beq $at,$zero,3764
3752#    and $t8,0xff # mask off hash bits
3753##...lui $at,0
3757##...ori $at,$at,0xff
3761##...and $t8,$t8,$at

3764:  gcSkip5:
3764#    sll $t8,$t8,2 # bytes in negative-offset portion ...
3768#    addu $t8,$t8,8 # ... add in bytes for header-words
3772#    subu $t4,$t1,$t8 # address of first word of source object
3776#    lw $t3,-4($t1) # # words in positive-offset portion
3780#    bge $t3,$zero,gcNoNeg
3781##...slt $at,$t3,$zero
3785##...beq $at,$zero,3792
3788#    move $t3,$zero # negative treated as zero
3792:  gcNoNeg:
3792#    sll $t3,$t3,2 # bytes in negative-offset portion
3796#    addu $t3,$t3,$t1 # address one word past last word of source

3800#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
3804#    lw $s0,($t4)
3808#    lw $t9,-8($t1)
3812#    sw $t9,($t4)
3816#    sw $s0,-8($t1)

    # copy all source bytes to the target
3820:  gCinnerCopy2:
3820#    lw $s0,($t4)
3824#    sw $s0,($s3)
3828#    addu $t4,$t4,4
3832#    addu $s3,$s3,4
3836#    bltu $t4,$t3,gCinnerCopy2
3837##...sltu $at,$t4,$t3
3841##...bne $at,$zero,3820

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3844#    sw $zero,-8($t1) # 0 in first header-slot
3848#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3852#    sw $t8,($t0)

3856:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3856#    addu $t0,$t0,4
3860#    bltu $t0,$s1,gcNextPointerInObject
3861##...sltu $at,$t0,$s1
3865##...bne $at,$zero,3704

3868:  gcTestIterDone2:
3868#    bltu $t0,$s3,gcHeapCopyLoop
3869##...sltu $at,$t0,$s3
3873##...bne $at,$zero,3588

    ###### end outer loop to copy all non-stack-direct objects ######

3876:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3876#    lw $s2,($sp)
3880#    addu $sp,$sp,4

    # mark us as having already GC'd
3884#    move $t5,$s5

    # go try and allocate again
3888#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3892:vm_init:

    # mark bottom of stack
3892#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3896#    move $s2,$zero

    #set up the "five" register
3900#    li $s5,5

3904#    la $s3,seg0_start
3908#    la $s4,seg0_end
3909##...lui $s4,16
3913##...addiu $s4,$s4,4772
    
    # return
3916#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3920:divide:
    # get parameters; abort if divisor zero
3920#    lw $t0,($sp)
3924#    lw $t1,8($sp)
3928#    beq $t0,$zero,divByZeroError
    
    # perform division
3932#    div $t1,$t0
3936#    mflo $t0

    # store result, adjust stack and return
3940#    addu $sp,$sp,8 # adjust stack
3944#    sw $t0,($sp) # store result
3948#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3952:remainder:
    # get parameters; abort if divisor zero
3952#    lw $t0,($sp)
3956#    lw $t1,8($sp)
3960#    beq $t0,$zero,divByZeroError
    
    # perform division
3964#    div $t1,$t0
3968#    mfhi $t0

    # store result, adjust stack and return
3972#    addu $sp,$sp,8 # adjust stack
3976#    sw $t0,($sp) # store result
3980#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3984:checkCast:
    # get object, allow immediately if null
3984#    lw $t2,($sp)
3988#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3992#    lw $t2,-12($t2) # vtable address
3996#    bge $t2,$t1,castException
3997##...slt $at,$t2,$t1
4001##...beq $at,$zero,4172
4004#    blt $t2,$t0,castException
4005##...slt $at,$t2,$t0
4009##...bne $at,$zero,4172
    
    # return, leaving object unchanged on stack
4012:  checkCastReturn:    
4012#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
4016:instanceOf:
    # get object, go return false if null
4016#    lw $t2,($sp)
4020#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
4024#    lw $t2,-12($t2) # vtable address
4028#    sge $t0,$t2,$t0 # are we >= the lower limit?
4029##...slt $t0,$t2,$t0
4033##...xori $t0,$t0,0x1
4036#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
4040#    and $t2,$t0,$t1
4044#    sw $t2,($sp)
4048:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
4048#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

4052:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
4052#    li $v0,4 # syscall-code for print-string
4056#    syscall
4060:exit:
    # print termination message
4060#    li $v0,4 # syscall-code for print-string
4064#    la $a0,exitString
4068#    syscall

    # print number of garbage collections
4072#    li $v0,4 # syscall-code for print-string
4076#    la $a0,gcCountMsg
4080#    syscall # print "Number of ..."
4084#    lw $a0,gcCount # number of GC's
4088#    li $v0,1 # code for print-int
4092#    syscall
4096#    li $v0,4 # syscall-code for print-string
4100#    la $a0,newlineString
4104#    syscall # print newline

    # terminate execution
4108#    li $v0,10 #syscall-code for "exit"
4112#    syscall

4116:outOfHeapMemory:
4116#    la $a0,heapMemoryMsg
4120#    j exitError
4124:divByZeroError:
4124#    la $a0,divByZeroMsg
4128#    j exitError
4132:strIndexOutOfBounds:
4132#    la $a0,strIndexOutOfBoundsMsg
4136#    j exitError
4140:arrayIndexOutOfBounds:
4140#    la $a0,arrayIndexOutOfBoundsMsg
4144#    j exitError
4148:arraySizeOutOfBounds:
4148#    la $a0,arraySizeOutOfBoundsMsg
4152#    j exitError
4156:nullPtrException:
4156#    la $a0,nullPtrExceptionMsg
4160#    j exitError
4164:badIntegerFormat:
4164#    la $a0,badIntegerFormatMsg
4168#    j exitError
4172:castException:
4172#    la $a0,castExceptionMsg
4176#    j exitError
############## data section ################
    .data
4400:hexChars:
    .ascii "0123456789abcdef"
    .align 2
4416:cur_seg:
    .word 0

4420:lastCharRead:
    .word -2 # -2 => no buffered character
4424:prevHashConstant:
    .word 0x2785173c
4428:gcCount:
    .word 0

4432:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
4459:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
4482:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
4517:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
4551:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
4584:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
4615:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
4663:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
4684:gcMsg: 
      .asciiz "\nGC!\n"
4690:gcCountMsg: 
      .asciiz "Number of garbage collections: "

4722:nullStr:
    .asciiz "null"
4727:trueString:
    .asciiz "true"
4732:falseString:
    .asciiz "false"
4738:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
4766:newlineString:
    .asciiz "\n"
    
    .align 2
4768:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

4772:seg0_start:
    .space 0x100000
1053348:seg0_end:
1053348:seg1_start:
    .space 0x100000
2101924:seg1_end:
